name: Vendera CI

on:
    push:
        branches:
            - main

jobs:
    test-ci:
        runs-on: ubuntu-latest

        steps:
            - name: Say hello 
              run: echo "Vendera CI pipeline is Working!"